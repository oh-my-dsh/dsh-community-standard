{
  "$comment": "场景化 fixture：非 schema 校验对象，由 conformance suites 驱动宿主执行 steps 并断言 expect。对应 spec/lifecycle.md §2.6。",
  "scenario": "ledger-no-secrets",
  "spec": "spec/lifecycle.md §2.6",
  "rule": "ledger 默认不得写入消息正文、secret、command argument 或任意插件 payload。",
  "steps": [
    { "action": "host.startup", "until": "ready" },
    { "action": "plugin.activate", "plugin": "com.example.alpha" },
    { "action": "broker.register", "by": "com.example.alpha", "effectKind": "command-handler", "args": { "token": "SECRET-VALUE-DO-NOT-LEAK" }, "note": "注册时携带含 secret 值的参数" }
  ],
  "expect": [
    "序列化后的 ledger 记录全文不含字符串 SECRET-VALUE-DO-NOT-LEAK",
    "记录仍包含定位所需的非敏感字段（effectId、契约坐标、resourceId、operation、state）",
    "如记录了裁剪行为，sensitivityClass 与裁剪策略字段如实反映"
  ]
}
