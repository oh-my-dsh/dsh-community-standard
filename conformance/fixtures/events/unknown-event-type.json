{
  "$comment": "场景化 fixture：非 schema 校验对象，由 conformance suites 驱动宿主执行 steps 并断言 expect。对应 spec/event-envelope.md §2.1。",
  "scenario": "unknown-event-type",
  "spec": "spec/event-envelope.md §2.1",
  "rule": "eventType / eventVersion 必须能解析到 Registry 精确条目；实现方不得自行发明等价事件名。",
  "steps": [
    { "action": "envelope.deliver", "envelope": { "eventType": "messages.observe", "eventVersion": "v1alpha1" }, "note": "把订阅用的事件名直接塞进 eventType，不是 Registry 条目的坐标两半" },
    { "action": "envelope.deliver", "envelope": { "eventType": "messages.dsh", "eventVersion": "v9" }, "note": "eventVersion 不存在于 Registry" },
    { "action": "envelope.deliver", "envelope": { "eventType": "messages.dsh", "eventVersion": "v1alpha1" }, "note": "对照组：可解析到 Registry 精确条目" }
  ],
  "expect": [
    "前两次投递被拒收并记录错误",
    "对照组投递正常送达"
  ]
}
